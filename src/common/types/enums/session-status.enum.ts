/**
 * Session status enumeration
 * Represents the different states a session can be in
 */
export enum SessionStatus {
  UPCOMING = 'upcoming',
  OPENING = 'opening',
  LIVE = 'live',
  COMPLETED = 'completed',
  TIME_REACHED = 'time_reached',
  CANCELLED = 'cancelled',
}
